# URL Cleaner Site WebSocket Client

A basic and experimental CLI client for URL Cleaner Site's WebSocket API.

Dramatically faster than WebSocat because it bundles multiple task lines into each message.

## Format

URL Cleaner Site WebSocket Client uses [the standard format](../format.md).
